-- List all records of second_table of hbtn_0c_0 database
-- Result should display both the score and the name
-- Records should order by score(top first)

SELECT score, name FROM second_table ORDER BY score DESC;
